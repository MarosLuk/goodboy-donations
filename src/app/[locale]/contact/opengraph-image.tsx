// A child openGraph object replaces the parent one rather than merging into it, so the
// image the locale segment generates does not reach this page on its own. Re-exported
// instead of copied, so there is still only one picture to maintain.
export { alt, contentType, default, size } from '../opengraph-image';
